apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: distributedregistry.azurecr.io/frontend:da5309bf0822afd32d6ac723a58bd647947e404f
        ports:
        - containerPort: 3000
        env:
        - name: REACT_APP_API_URL
          value: "/" # Relative path because Nginx handles routing
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
  - port: 3000
    targetPort: 3000
